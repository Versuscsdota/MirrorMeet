# MirrorCRM - VPS production environment

# App
NODE_ENV=production
PORT=8080

# Data locations (ensure directories exist and are writable by the app user)
KV_SQLITE_PATH=/var/lib/mirrorcrm/data.sqlite
FILES_DIR=/var/lib/mirrorcrm/files

# Session / cookies
# Keep cookie name stable to avoid multiple cookies after migrations
SESSION_COOKIE_NAME=mirrorsid
# 7 days in seconds
SESSION_TTL_SECONDS=604800
# REQUIRED: set a strong secret (hex 32-64). Example: `openssl rand -hex 32`
SESSION_HMAC_SECRET=23aabc448775adfc683726d2980676044c856fefcc2a8dee235351246d2ee80b
